version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
      - DJANGO_SETTINGS_MODULE=donation.settings
      - ALLOWED_HOSTS=*
    volumes:
      - .:/app
    command: python manage.py runserver 0.0.0.0:8000
